@using KenticoKontentModels
@model Homepage
@{
    Layout = null;
}

<ul>
    <li>
        <a asp-controller="Home" asp-action="Index">@Model.Title</a>
    </li>
@foreach (Page page in Model.Subpages)
{
    if (page.ShowInNavigation.FirstOrDefault().Codename == "yes")
    {
        @Html.DisplayFor(vm => page, "NavigationLevelOne")
    }
}
</ul>
